import PageHeader from "../components/layout/PageHeader";

export default function PostsPage() {
  return (
    <div>
      <PageHeader
        title="Posts"
        description="Browse fetched posts and manage generation workflow."
      />
      <div className="card">
        Posts page is ready for next step.
      </div>
    </div>
  );
}
